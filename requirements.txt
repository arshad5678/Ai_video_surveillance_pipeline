# --- Web API -----------------------------------------------------------
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
gunicorn>=22.0,<23.0            # production ASGI process manager for containers
python-multipart>=0.0.9         # multipart/form-data support (future video upload endpoint)

# --- Config, validation, env --------------------------------------------
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0
PyYAML>=6.0,<7.0

# --- Logging -------------------------------------------------------------
loguru>=0.7,<1.0

# --- Computer vision / ML --------------------------------------------
numpy>=1.26,<2.0
opencv-python>=4.9,<5.0
Pillow>=10.3,<11.0
ultralytics>=8.2,<9.0           # YOLO detector
torch>=2.2,<3.0
torchvision>=0.17,<1.0

# --- Tracking --------------------------------------------------------
lap>=0.4.0                      # linear assignment, used by ByteTrack-style matching
scipy>=1.11,<2.0                # Kalman filter / assignment utilities
filterpy>=1.4.5                 # Kalman filter (DeepSORT-style motion model)
deep-sort-realtime>=1.3.2       # DeepSORT tracker + appearance embedder

# --- Geometry (zone / point-in-polygon math) --------------------------
shapely>=2.0,<3.0

# --- Persistence -------------------------------------------------------
SQLAlchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg2-binary>=2.9,<3.0       # PostgreSQL driver for production deployments

# --- Misc utilities ------------------------------------------------------
tqdm>=4.66,<5.0                 # progress bars for batch/video processing
orjson>=3.10,<4.0                # fast JSON responses
psutil>=5.9,<6.0                 # process CPU/memory usage for the System router
