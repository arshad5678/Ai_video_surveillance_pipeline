"""Aggregates versioned business endpoints (events, tracks, zones, videos).

Empty for now — populated as surveillance pipeline endpoints are implemented.
"""

from fastapi import APIRouter

api_router = APIRouter()
