-r requirements.txt

# --- Testing -------------------------------------------------------------
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0,<6.0
httpx>=0.27,<1.0                # FastAPI TestClient / async test client

# --- Lint / type-check / formatting --------------------------------------
ruff>=0.5,<1.0
mypy>=1.10,<2.0

# --- Local dev QoL ---------------------------------------------------
pre-commit>=3.7,<4.0
